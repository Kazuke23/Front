name: Deploy Angular to S3

on:
  push:
    branches:
      - main   #v22

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build Angular for S3
        run: |
          npx ng build --configuration production --base-href ./

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to S3
        run: |
          echo "🚀 Desplegando desde: dist/frontend/browser/"
          aws s3 sync dist/frontend/browser/ s3://${{ secrets.S3_BUCKET_NAME }} --delete

      - name: Configure S3 for SPA
        run: |
          echo "🌐 Configurando S3 para Single Page Application..."
          aws s3 website s3://${{ secrets.S3_BUCKET_NAME }} \
            --index-document index.html \
            --error-document index.html

      - name: Verify deployment
        run: |
          echo "📊 Archivos desplegados en S3:"
          aws s3 ls s3://${{ secrets.S3_BUCKET_NAME }} --recursive --human-readable
          echo "🎉 ¡Despliegue exitoso!"
          echo "🌐 URL: http://${{ secrets.S3_BUCKET_NAME }}.s3-website.${{ secrets.AWS_REGION }}.amazonaws.com"
